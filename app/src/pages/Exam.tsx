import type { JSX } from 'preact';
import type { Route } from '../lib/router';

export function ExamPage(_props: { route: Route }): JSX.Element {
  return <div class="panel">Exam practice coming in Stage 5.</div>;
}
