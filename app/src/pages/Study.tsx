import type { JSX } from 'preact';
import type { Route } from '../lib/router';

export function StudyPage(_props: { route: Route }): JSX.Element {
  return <div class="panel">Decks coming in Stage 3.</div>;
}
