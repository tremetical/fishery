import type { JSX } from 'preact';
import type { Route } from '../lib/router';

export function RadioPage(_props: { route: Route }): JSX.Element {
  return <div class="panel">Radio trainer coming in Stage 3.</div>;
}
