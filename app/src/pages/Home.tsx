import type { JSX } from 'preact';

export function HomePage(): JSX.Element {
  return (
    <div class="stack">
      <h1 class="page-h">
        Preflight
        <span class="sub">Stage 1 scaffold — study engine arrives next.</span>
      </h1>
    </div>
  );
}
