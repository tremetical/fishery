# Deploys the Preflight PWA to GitHub Pages.
# One-time setup: repo Settings → Pages → Source: "GitHub Actions".
# The app then lives at https://<user>.github.io/fishery/
name: Deploy Preflight

on:
  push:
    # Deploys from main OR from the working branch, so the app can be
    # installed on a phone without merging first.
    branches: [main, 'claude/**']
    paths: ['app/**', '.github/workflows/deploy-preflight.yml']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json
      - run: npm ci
        working-directory: app
      - run: npm test
        working-directory: app
      - run: npm run build
        working-directory: app
        env:
          BASE: /${{ github.event.repository.name }}/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
